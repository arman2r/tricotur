import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tricotur.app',
  appName: 'tricotur',
  webDir: 'src',
  bundledWebRuntime: false
};

export default config;
